<Window x:Class="Wpf_ctrls_builder_ctrls.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_ctrls_builder_ctrls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid RenderTransformOrigin="0.403,0.518">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <!--Tooltip控件-->
        <Button Content="ToolTip button" Grid.Row="0" Grid.Column="0" Margin="5">
            <Button.ToolTip>
                <ToolTip Background="GreenYellow" Foreground="White" HasDropShadow="True" Placement="MousePoint">
                    <TextBlock Text="ToolTip控件的用法" Margin="5"/>
                </ToolTip>
            </Button.ToolTip>
        </Button>
        <!--Thumb控件-->
        <Canvas Background="LightGreen" Grid.Row="0" Grid.Column="1" Name="canvas">
            <Thumb Name="thumd1" Width="20" Height="15" Background="BlanchedAlmond" Canvas.Left="200"
                   Canvas.Top="65" DragStarted="ThumdStart" DragDelta="ThumdMove"
                  />

        </Canvas>
        <!--Border控件-->
        <Canvas Grid.Row="1" Grid.Column="0">
            <Border Canvas.Left="12" Canvas.Top="5" BorderThickness="5" CornerRadius="0" BorderBrush="green" Padding="10">
                <TextBox>WPF Border</TextBox>
            </Border>
            <Border Canvas.Left="135" Canvas.Top="5" BorderThickness="5,3,5,3" CornerRadius="0,18,0,18" BorderBrush="green" Padding="10">
                <TextBox>WPF Border</TextBox>
            </Border>
            <Border Canvas.Left="249" Canvas.Top="5" BorderThickness="5" CornerRadius="8" BorderBrush="green" Padding="10">
                <TextBox>WPF Border</TextBox>
            </Border>
            <Border Canvas.Left="12" Canvas.Top="70" BorderThickness="15,5,15,5" CornerRadius="16" BorderBrush="green" Padding="10">
                <TextBox>WPF Border</TextBox>
            </Border>
        </Canvas>
        <!--Popup控件,有弹出框和弹出菜单-->
        <StackPanel Grid.Row="1" Grid.Column="1">
            <Button Click="Button_Click" Content="点击弹出Popup" Margin="20" />
            <Popup Name="popup" PopupAnimation="Fade" Placement="Mouse">
                <Button Content="Popup Box"/>
            </Popup>
            <!--Popup Menu-->
            <Button x:Name="cmd" Content="Popup Menu" Margin="30,20">
                <Button.ContextMenu>
                    <ContextMenu x:Name="menu">
                        <MenuItem Header="submenu0">
                            <MenuItem Header="subsubmenu0"/>
                            <MenuItem Header="subsubmenu1"/>
                            <MenuItem Header="subsubmenu2"/>
                        </MenuItem>
                        <MenuItem Header="submenu1" />
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
        </StackPanel>
        <!--ScrollView控件-->
        <ScrollViewer Grid.Row="2" Grid.Column="0" HorizontalScrollBarVisibility="Auto" 
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <TextBox Margin="3" Grid.Row="0"  Name="toAdd"/>
                <Button  Margin="3" Grid.Row="1" Name="add" Content="Add" Click="AddItem" />
                <ListBox Margin="3"  Grid.Row="2" Name="list1" />
            </Grid>
        </ScrollViewer>
        <!--ViewBox控件-->
        <Grid Grid.Row="2" Grid.Column="1">
            <Viewbox VerticalAlignment="Top">
                <TextBlock Text="WPF ViewBox.TextBlock"/>
            </Viewbox>
            <Viewbox VerticalAlignment="Bottom">
                <Button Content="WPF ViewBox.Button"/>
            </Viewbox>
        </Grid>
    </Grid>
</Window>

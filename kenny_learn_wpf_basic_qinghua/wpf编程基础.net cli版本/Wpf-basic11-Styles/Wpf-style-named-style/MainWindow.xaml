<Window x:Class="Wpf_style_named_style.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_style_named_style"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="style1" TargetType="Control">
            <Setter Property="FontSize" Value="40" />
            <Setter Property="Width" Value="250" />
            <Setter Property="Height" Value="90" />
            <Setter Property="Background" Value="Gold" />
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="Foreground" Value="DeepPink" />
        </Style>
    </Window.Resources>
    <StackPanel>
        <Button Margin="5" Style="{StaticResource style1}" Content="Button"/>
        <CheckBox Margin="10" Style="{StaticResource style1}" Content="CheckBox1"/>
        <RadioButton Margin="10" Style="{StaticResource style1}" Content="Radi01"/>
        <Label Margin="5"  Style="{StaticResource style1}" Content="Label1" />
    </StackPanel>
</Window>

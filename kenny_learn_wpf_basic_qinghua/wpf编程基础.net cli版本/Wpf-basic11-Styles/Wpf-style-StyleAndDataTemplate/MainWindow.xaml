<Window x:Class="Wpf_style_StyleAndDataTemplate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_style_StyleAndDataTemplate"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="900">
    <Window.Resources>
        <Style TargetType="ListBoxItem" x:Key="ListViewItemStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem" x:Name="ListBorder">
                        <Border BorderBrush="Gold" BorderThickness="1" x:Name="ss">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="190"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Image Source="{Binding Path=Phote}" Width="150" Grid.Column="0" Grid.RowSpan="1"/>
                                <Label Content="{Binding Path=Name}" Grid.Column="1" Grid.ColumnSpan="1" FontFamily="微软雅黑"
                                    FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="SkyBlue" TargetName="ss"/>
                            </Trigger> 
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="Violet" TargetName="ss"/>
                            </Trigger>
                            
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <ListBox Background="Transparent" Margin="0,5,0,5" BorderBrush="Transparent"
                 ItemsSource="{Binding Path=Persons}" ItemContainerStyle="{StaticResource ListViewItemStyle}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Window>

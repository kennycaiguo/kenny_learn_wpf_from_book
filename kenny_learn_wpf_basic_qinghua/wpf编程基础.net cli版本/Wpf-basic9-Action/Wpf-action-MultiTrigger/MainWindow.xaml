<Window x:Class="Wpf_action_MultiTrigger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_action_MultiTrigger"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="{x:Type CheckBox}">
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsChecked" Value="True" />
                        <Condition Property="Content" Value="浪花淘尽英雄" />
                    </MultiTrigger.Conditions>
                    <MultiTrigger.Setters>
                        <Setter Property="FontSize" Value="35"/>
                        <Setter Property="Foreground" Value="DeepPink"/>
                    </MultiTrigger.Setters>
                </MultiTrigger>
            </Style.Triggers> 
        </Style>
    </Window.Resources>
    <StackPanel>
        <CheckBox Content="滚滚长江东逝水" Margin="5" />
        <CheckBox Content="浪花淘尽英雄" Margin="5" />
        <CheckBox Content="青山依旧在" Margin="5" />
        <CheckBox Content="几度夕阳红" Margin="5" />
    </StackPanel>
</Window>

<Window x:Class="Wpf_action_MultiDataTrigger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_action_MultiDataTrigger"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="Button">
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Path=Name}" Value="梦想成真" />
                        <Condition Binding="{Binding Path=Age}" Value="99" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="FontSize" Value="48" />
                    <Setter Property="Foreground" Value="DeepSkyBlue" />
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid x:Name="grid1">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Height="23" Grid.Row="0" Grid.Column="0" Text="Name:" HorizontalAlignment="Left"
                   Margin="12,12,0,0" VerticalAlignment="Center"/> 
        <TextBlock Height="23" Grid.Row="1" Grid.Column="0" Text="Age:" HorizontalAlignment="Left"
                   Margin="12,12,0,0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Name}" HorizontalAlignment="Center"
                 Width="120" Margin="0,12,12,0" Name="nameTextBox" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Path=Age}" HorizontalAlignment="Center"
                 Width="120" Margin="0,12,12,0" Name="ageTextBox" VerticalAlignment="Center" />
        <Button Content="{Binding Path=Name}" Name="button1" Grid.Row="2" Grid.ColumnSpan="2" Margin="12"/>
    </Grid>
</Window>
